spring:
  redis:
    database: 1 # 数据库索引
    host: localhost
    port: 6379
    password: yingzigeniub666
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/album?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
      password: qiaozhiling
      username: qiao
      connection-error-retry-attempts: 3
      login-timeout: 3000
      kill-when-socket-read-timeout: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB
#  mvc:
#    pathmatch:
#      matching-strategy: ant_path_matcher

mybatis:
  type-aliases-package: com.qiao.album.pojo
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: true

server:
  port: 8081
  white: /user/token, /user/new, /, /v2/api-docs, /api/*, /webjars/*, /swagger*
  servlet:
    context-path: /a
    encoding:
      charset: UTF-8

myjwt:
  expire: 604800 # 七天 单位s
  secret: hjl43L*o3#OG904_Ggo@#SOihj
  startWith: ""
  header: Authorization
  redisKey: 'login:'

images:
  path: 'C:\wwwroot\qiao'

swagger:
  enable: true
  application-name: 云相册
  application-version: 1.0
  application-description: 云相册

logging:
  level:
    root: info
    io.swagger.models.parameters.AbstractSerializableParameter: error